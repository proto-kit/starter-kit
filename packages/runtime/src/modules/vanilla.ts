import { VanillaRuntimeModules } from "@proto-kit/library";

/**
 * Vanilla runtime modules, such as "Balances"
 */
export const modules = VanillaRuntimeModules.with({});
export const defaultConfig = VanillaRuntimeModules.defaultConfig();
